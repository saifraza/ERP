{
  "name": "@erp/mcp-server",
  "version": "0.0.1",
  "type": "module",
  "description": "MCP Server for ERP Factory Operations",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir=dist",
    "start": "bun run dist/index.js",
    "deploy": "railway up",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@types/node": "^20.14.0",
    "zod": "^3.24.1",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}